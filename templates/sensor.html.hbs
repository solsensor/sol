<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}}</h1>
	<p>This page shows details about the sensor "{{sensor.id}}"</p>
	<ul>
	  <li>id: {{sensor.id}}</li>
	  <li>owner_id: {{sensor.owner_id}}</li>
	  <li>hardware_id: {{sensor.hardware_id}}</li>
	  <li>
		readings:
		<ul>
		  {{#each readings}}
		  <li>id={{id}}, voltage={{voltage}}</li>
		  {{/each}}
		</ul>
	  </li>
	</ul>
  </body>
</html>
